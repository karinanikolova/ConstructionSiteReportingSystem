@model IEnumerable<WorkTypeServiceModel>

@{
	ViewData["Title"] = "Construction and Assembly Work Type Suggestions for Approval";

	int n = 1;
}

<div class="mb-5">
	<h3 class="text-center">@ViewData["Title"]</h3>
</div>

<table class="table table-sm">
	<thead class="table-light">
		<tr>
			<th scope="col">#</th>
			<th scope="col">Name</th>
			<th scope="col"></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var workType in Model)
		{
			<tr>
				<th scope="row">@n</th>
				<td>@workType.Name</td>
				<td>
					<div class="btn-group" role="group" aria-label="Basic outlined example">
						<form asp-controller="WorkType" asp-action="ApproveWorkType" asp-area="Admin" method="post">
							<input type="hidden" name="id" value="@workType.Id" />
							<button type="submit" class="btn btn-outline-primary">Approve</button>
						</form>
						<a asp-controller="WorkType" asp-action="EditWorkType" asp-route-id="@workType.Id" class="btn btn-outline-primary" type="button">Edit</a>
						<form asp-controller="WorkType" asp-action="RemoveWorkType" asp-area="Admin" method="post">
							<input type="hidden" name="id" value="@workType.Id" />
							<button type="submit" class="btn btn-outline-primary">Remove</button>
						</form>
					</div>
				</td>
			</tr>
			n++;
		}
	</tbody>
</table>